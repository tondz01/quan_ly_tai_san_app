import 'package:flutter/material.dart';

class OwnershipUnitDetails extends StatelessWidget {
  const OwnershipUnitDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}